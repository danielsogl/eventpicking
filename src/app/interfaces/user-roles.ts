/**
 * User roles interface
 * @author Daniel Sogl
 */
export interface UserRoles {
  /**
   * Admin role
   */
  admin?: boolean;
  /**
   * Photographer role
   */
  photographer?: boolean;
  /**
   * User role
   */
  user?: boolean;
}
